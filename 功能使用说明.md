# SEO数据分析工具功能使用说明

## 📋 目录

1. [服务启动指南](#服务启动指南)
2. [数据分析功能](#数据分析功能)
3. [排名查询功能](#排名查询功能)
4. [关键词查询功能](#关键词查询功能)
5. [历史报告查看](#历史报告查看)
6. [后台任务系统](#后台任务系统)
7. [Session历史记录管理](#session历史记录管理)
8. [排名查询优化](#排名查询优化)

---

## 🚀 服务启动指南

### 概述

本项目包含两个主要服务：
- **数据分析服务** (端口5001)：关键词排名分析工具
- **排名查询服务** (端口5002)：独立的排名查询工具

### 快速启动

#### 方式一：Shell脚本（推荐）
```bash
# 启动所有服务
./start_services.sh

# 检查服务状态
./start_services.sh status

# 停止所有服务
./start_services.sh stop
```

#### 方式二：Python脚本
```bash
# 启动所有服务
python start_services.py start

# 检查服务状态
python start_services.py status

# 停止所有服务
python start_services.py stop
```

### 服务访问地址
- **数据分析工具**: http://127.0.0.1:5001
- **排名查询工具**: http://127.0.0.1:5002

---

## 📊 数据分析功能

### 概述
数据分析服务提供Excel文件处理、关键词排名分析、对比分析等功能。

### 主要功能

#### 1. Excel文件上传与处理
- 支持xlsx/xls格式文件
- 智能列名映射（UV/PV/URL等）
- 自动数据格式标准化

#### 2. 排名分析
- 基本统计指标计算
- 同比/环比对比分析
- 下降词智能识别（UV>10且降幅>50%）
- 考种自动分类统计

#### 3. 报告生成
- Excel格式分析报告
- 可视化图表生成
- 优化建议输出

---

## 🔍 排名查询功能

### 概述
排名查询服务支持实时关键词排名查询、批量处理、结果导出等功能。

### 主要功能

#### 1. 目标下降词一键查询
- 自动提取UV大于10且降幅大于50%的目标下降词
- 一键查询多达492个关键词的排名
- 显示详细的流量变化信息
- 按下降流量排序，优先显示影响最大的词

#### 2. 手动URL查询
- 支持批量输入URL（最多10个）
- 实时排名查询
- 结果导出功能

#### 3. 智能关键词提取
- 从分析结果自动提取关键词
- 优先从页面meta keywords标签提取
- 备用方案：使用页面标题提取

---

## 🎯 关键词查询功能

### 概述
系统支持从最新的Excel分析报告中自动提取目标下降词，并使用每个URL页面的keywords标签进行排名查询。

### 主要特性

#### 🔍 智能关键词提取
- 自动从URL页面的`<meta name="keywords" content="...">`标签中提取关键词
- 如果没有keywords标签，自动使用页面标题作为备用关键词
- 支持关键词格式清理和优化

#### 📊 实时进度跟踪
- 显示详细的查询进度（提取关键词、查询排名）
- 实时显示当前处理的URL和完成百分比
- 支持后台异步处理，不阻塞用户界面

#### ⚡ 超时优化处理
- 关键词提取设置8秒超时，避免长时间等待
- 查询失败自动重试机制

---

## 📋 历史报告查看

### 概述
系统支持在线查看历史分析报告，无需下载Excel文件即可直接在浏览器中查看报告内容。

### 主要功能

#### 1. 首页历史报告列表
- 自动显示最近生成的分析报告
- 显示报告生成时间和文件大小
- 提供"在线查看"和"下载"两种操作方式
- 最多显示10个最新报告

#### 2. 在线查看功能
- **多标签页显示**：每个Excel工作表对应一个标签页
- **数据统计**：显示每个工作表的行数和列数
- **响应式表格**：支持横向和纵向滚动
- **数据格式化**：数字列自动右对齐，百分比显示

#### 3. 搜索和筛选
- 支持关键词搜索
- 按日期范围筛选报告
- 快速定位特定数据

---

## ⚙️ 后台任务系统

### 概述
后台任务系统专为大批量排名查询而设计，支持任务队列管理、失败重试、进度监控等功能。

### 主要功能

#### 1. 后台任务执行
- **异步处理**：任务在后台执行，不会阻塞用户界面
- **持久化**：任务状态自动保存，服务重启后可恢复
- **队列管理**：支持多个任务并行执行

#### 2. 智能重试机制
- **自动重试**：查询失败后自动重试，最多重试3次
- **重试间隔**：重试间隔>70秒，避免API限制
- **失败处理**：达到最大重试次数后标记为失败

#### 3. 任务状态管理
- **实时进度**：显示任务执行进度和当前查询项
- **状态跟踪**：PENDING → RUNNING → SUCCESS/FAILED
- **历史记录**：保存任务执行历史和结果

---

## 💾 Session历史记录管理

### 概述
Session管理系统提供持久化存储功能，让动态结果页面可以长期重复查看，并保存关键词提取和排名查询的历史记录。

### 主要功能

#### 1. 会话持久化
- **JSON格式存储**：轻量级数据持久化
- **自动时间戳**：完整的操作历史记录
- **分类管理**：关键词提取、排名查询分类存储

#### 2. 历史记录查看
- **关键词历史**：保存在`sessions/keyword_history/`目录
- **排名历史**：保存在`sessions/rank_history/`目录
- **快速访问**：通过历史记录快速查看之前的分析结果

#### 3. 数据管理
- **自动清理**：定期清理过期会话数据
- **存储优化**：压缩存储，节省磁盘空间
- **备份恢复**：支持会话数据备份和恢复

---

## 🚀 排名查询优化

### 主要改进

#### 1. 排名显示优化
- **问题**：排名1-3显示不够直观
- **解决**：排名1-3现在显示为"1页第X名"，其他排名显示为"X页第Y名"
- **示例**：
  - 排名1 → "1页第1名"
  - 排名15 → "2页第5名"
  - 排名0 → "未上榜"

#### 2. 查询结果持久化
- **问题**：中断后无法继续查询
- **解决**：
  - 每个关键词查询后立即保存状态
  - 支持中断后继续查询
  - 详细的进度统计和状态管理
  - 查询历史记录

#### 3. 性能优化
- **并发控制**：智能调整并发查询数量
- **缓存机制**：30分钟缓存，减少重复查询
- **API限流**：动态调整查询间隔，避免API限制

#### 4. 用户体验提升
- **实时反馈**：查询进度实时更新
- **错误处理**：友好的错误提示和恢复建议
- **结果导出**：支持多种格式导出查询结果

---

## 🔧 故障排除

### 常见问题

#### 1. 服务无法启动
```bash
# 检查端口占用
lsof -i :5001
lsof -i :5002

# 检查日志文件
tail -f data_analysis.log
tail -f rank_query.log
```

#### 2. API配置问题
- 确保已正确配置`api_keys.json`文件
- 检查站长之家API Key是否有效
- 验证网络连接是否正常

#### 3. 文件上传失败
- 检查上传目录权限
- 确保文件格式正确（xlsx/xls）
- 检查文件大小是否超过限制

#### 4. 查询结果异常
- 清除缓存重新查询
- 检查API调用次数是否超限
- 验证目标URL是否可访问

---

## 📞 技术支持

如有问题，请：
1. 查看相关日志文件
2. 检查配置文件是否正确
3. 参考项目文档：`项目介绍.md`、`部署指南.md`
4. 联系技术团队获取支持

---

*文档最后更新：2025-01-23*