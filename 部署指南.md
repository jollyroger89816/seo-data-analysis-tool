# SEO数据分析工具部署指南

## 目标服务器
- **IP地址**: 120.48.54.112
- **系统**: Ubuntu/CentOS
- **用户**: root

## 前置条件

### 1. SSH密钥配置
确保可以通过SSH无密码登录服务器：
```bash
# 如果还没有SSH密钥，先生成一个
ssh-keygen -t rsa -b 4096 -C "your_email@example.com"

# 将公钥复制到服务器
ssh-copy-id root@120.48.54.112

# 测试SSH连接
ssh root@120.48.54.112 "echo 'SSH连接成功'"
```

### 2. 服务器基础环境
服务器需要安装以下基础软件：
- Python 3.8+
- pip
- nginx
- systemd

## 部署步骤

### 方法一：自动化部署（推荐）

1. **执行部署脚本**
   ```bash
   chmod +x deploy.sh
   ./deploy.sh
   ```

2. **等待部署完成**
   脚本会自动：
   - 检查必要文件
   - 创建部署包
   - 上传到服务器
   - 安装依赖
   - 配置systemd服务
   - 启动服务

### 方法二：手动部署

1. **创建部署包**
   ```bash
   tar -czf seo_analysis.tar.gz \
     *.py templates/ static/ \
     requirements.txt config.json \
     start_services.sh nginx.conf
   ```

2. **上传到服务器**
   ```bash
   scp seo_analysis.tar.gz root@120.48.54.112:/tmp/
   ```

3. **在服务器上部署**
   ```bash
   ssh root@120.48.54.112
   
   # 创建部署目录
   mkdir -p /opt/seo_analysis
   cd /opt/seo_analysis
   
   # 解压文件
   tar -xzf /tmp/seo_analysis.tar.gz
   
   # 安装Python环境
   apt-get update
   apt-get install -y python3 python3-pip python3-venv nginx
   
   # 创建虚拟环境
   python3 -m venv venv
   source venv/bin/activate
   
   # 安装依赖
   pip install -r requirements.txt
   
   # 创建必要目录
   mkdir -p sessions uploads exports logs
   
   # 配置nginx
   cp nginx.conf /etc/nginx/sites-available/seo_analysis
   ln -s /etc/nginx/sites-available/seo_analysis /etc/nginx/sites-enabled/
   nginx -t && systemctl restart nginx
   
   # 启动服务
   python start_services.py
   ```

## 服务配置

### systemd服务文件

**数据分析服务** (`/etc/systemd/system/seo-analysis-data.service`):
```ini
[Unit]
Description=SEO Analysis Data Service
After=network.target

[Service]
Type=simple
User=root
WorkingDirectory=/opt/seo_analysis
Environment=PYTHONPATH=/opt/seo_analysis
Environment=FLASK_ENV=production
ExecStart=/opt/seo_analysis/venv/bin/python keyword_rank_analyzer.py
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

**排名查询服务** (`/etc/systemd/system/seo-analysis-rank.service`):
```ini
[Unit]
Description=SEO Analysis Rank Query Service
After=network.target

[Service]
Type=simple
User=root
WorkingDirectory=/opt/seo_analysis
Environment=PYTHONPATH=/opt/seo_analysis
Environment=FLASK_ENV=production
ExecStart=/opt/seo_analysis/venv/bin/python rank_query_app.py
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

### 启用和启动服务
```bash
systemctl daemon-reload
systemctl enable seo-analysis-data
systemctl enable seo-analysis-rank
systemctl start seo-analysis-data
systemctl start seo-analysis-rank
```

## 验证部署

### 1. 检查服务状态
```bash
# 检查服务状态
systemctl status seo-analysis-data
systemctl status seo-analysis-rank

# 检查端口监听
netstat -tlnp | grep -E ':500[12]'

# 检查日志
journalctl -u seo-analysis-data -f
journalctl -u seo-analysis-rank -f
```

### 2. 测试访问
```bash
# 测试数据分析服务
curl http://120.48.54.112:5001/

# 测试排名查询服务
curl http://120.48.54.112:5002/

# 测试nginx代理
curl http://120.48.54.112/
curl http://120.48.54.112/rank
```

### 3. 访问Web界面
- **数据分析工具**: http://120.48.54.112:5001
- **排名查询工具**: http://120.48.54.112:5002
- **通过nginx**: http://120.48.54.112

## 防火墙配置

```bash
# 开放必要端口
ufw allow 22    # SSH
ufw allow 80    # HTTP
ufw allow 443   # HTTPS
ufw allow 5001  # 数据分析服务
ufw allow 5002  # 排名查询服务
ufw enable
```

## 常见问题

### 1. 服务无法启动
```bash
# 检查日志
journalctl -u seo-analysis-data -n 50
journalctl -u seo-analysis-rank -n 50

# 检查端口占用
netstat -tlnp | grep -E ':500[12]'

# 手动启动测试
cd /opt/seo_analysis
source venv/bin/activate
python keyword_rank_analyzer.py
```

### 2. 无法访问Web界面
```bash
# 检查nginx状态
systemctl status nginx

# 检查nginx配置
nginx -t

# 检查防火墙
ufw status
```

### 3. 文件上传失败
```bash
# 检查上传目录权限
ls -la /opt/seo_analysis/uploads/

# 修改权限
chown -R www-data:www-data /opt/seo_analysis/uploads/
chmod 755 /opt/seo_analysis/uploads/
```

## 维护命令

```bash
# 重启服务
systemctl restart seo-analysis-data
systemctl restart seo-analysis-rank

# 查看日志
tail -f /var/log/nginx/seo_analysis_access.log
tail -f /var/log/nginx/seo_analysis_error.log

# 更新代码
cd /opt/seo_analysis
git pull  # 如果使用git
systemctl restart seo-analysis-data
systemctl restart seo-analysis-rank

# 备份数据
tar -czf seo_analysis_backup_$(date +%Y%m%d).tar.gz \
  /opt/seo_analysis/sessions/ \
  /opt/seo_analysis/uploads/ \
  /opt/seo_analysis/exports/
```

## 监控建议

1. **设置监控脚本**
2. **配置日志轮转**
3. **定期备份数据**
4. **监控服务器资源**

## 联系方式

如有问题，请联系系统管理员。 